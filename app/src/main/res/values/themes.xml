<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 🌿🌻 Tema brasileiro do Movie Explorer 🌻🌿 -->
    <style name="Base.Theme.MovieExplorer" parent="android:Theme.Holo.Light">
        <!-- Cores primárias brasileiras -->
        <item name="android:colorPrimary">@color/brazil_green</item>
        <item name="android:colorPrimaryDark">@color/deep_forest_green</item>
        <item name="android:colorAccent">@color/brazil_yellow</item>
        
        <!-- Cores de background -->
        <item name="android:windowBackground">@color/cream_yellow</item>
        <item name="android:colorBackground">@color/cream_yellow</item>
        
        <!-- Cores de texto -->
        <item name="android:textColorPrimary">@color/dark_gray</item>
        <item name="android:textColorSecondary">@color/neutral_gray</item>
    </style>

    <style name="Theme.MovieExplorer" parent="Base.Theme.MovieExplorer" />
</resources>